# Bootstrap Prompt Notice

For this repository, use the root-level `CODEX_PROJECT_LEAD_PROMPT.md` as the initial prompt.

The project is running in Lead mode: Codex first completes Gate 0 resource discovery, then executes the two-week plan as sequential, tested, separately committed vertical slices. The root `AGENTS.md` overrides any older instruction that would require the user to manually prompt every day.
