# Durable Project Decisions

> Append only. Record decisions that future Codex turns must know without rereading chat history.

| Date | ID | Decision | Reason | Scope impact | Approved by |
|---|---|---|---|---|---|
| 2026-07-26 | DEC-001 | P0 one week, P1 one week; R11 only after P0/P1 acceptance and if time remains | User-approved delivery constraint | Locks scope and priority | Forrest Zhang |
| 2026-07-26 | DEC-002 | Use the stack and boundaries in `docs/spec/00_CODEX_START_HERE.md` | Reduce research and implementation churn | Architecture locked | Forrest Zhang |
| 2026-07-26 | DEC-003 | One persistent Codex lead executes sequential vertical slices; separate commits and human gates remain | Minimize user prompting while preserving reviewability | Delivery workflow | Forrest Zhang |
| 2026-07-26 | DEC-004 | Product display name is `SmartService`, repository/package slug is `smartservice`, and new Cloud resources use `smartservice-*` names | Direct Gate 0 identity instruction | Documentation, packages, UI, deployment resources | Forrest Zhang |
| 2026-07-26 | DEC-005 | Historical source PDFs retain their original Chinese titles; repository-owned specifications use SmartService identity | Preserve source provenance while removing obsolete working-name drift | Documentation only | Forrest Zhang |
| 2026-07-26 | DEC-006 | Fictional fixtures are the default demo tenant; real customer, production, regulated, or sensitive data is not permitted without a separate review | Keeps the sales demo reproducible and low risk | Demo data and privacy | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-007 | The default G1 runtime is local unless an externally reachable preview is explicitly approved; a bare `workers.dev` URL is non-production but not private | Avoid accidentally exposing a supposedly private preview | Deployment and security | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-008 | If a preview is approved, prefer one Cloudflare Worker with Static Assets and the Hono API under one origin; Queue consumers and provider modules remain inside the modular monolith | Reduces CORS, version, and deployment drift without changing the locked stack | P0 deployment topology | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-009 | P1 `<1.5s` is the P95 warm-turn target, not a universal guarantee; missing it requires a complete honest latency report and explicit G2 acceptance rather than hidden/selective results | Resolves source wording against the controlling prompt and acceptance specification | Voice acceptance | Forrest Zhang via controlling prompt |
| 2026-07-26 | DEC-010 | Canonical model variables are `OPENAI_CHAT_MODEL`, `OPENAI_SUPERVISOR_MODEL`, `OPENAI_EMBEDDING_MODEL`, and `OPENAI_EMBEDDING_DIMENSIONS`; model aliases remain configurable and undated | Removes conflicting environment names and prevents unavailable snapshot pins | Configuration | Project Lead |
| 2026-07-26 | DEC-011 | Cloudflare `/crawl` uses the REST API and a least-privilege Browser Run token; a Browser binding is not treated as equivalent | Matches the current provider contract and makes credentials auditable | URL ingestion | Project Lead |
| 2026-07-26 | DEC-012 | Firecrawl remains an inactive optional adapter; a real account or usage requires a demonstrated Browser Run blocker and separate provider/cost approval | Preserves the locked provider choice and avoids scope/cost creep | URL ingestion fallback | Project Lead |
| 2026-07-26 | DEC-013 | Audio recording remains disabled; application records default to the documented 30-day demo retention unless Gate 0 changes it | Minimizes privacy exposure while retaining the specification default | Privacy and data lifecycle | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-014 | Root `.env.local` is the canonical local secret source; app-specific ignored files or explicit workspace configuration may consume it, but no secret is copied into tracked configuration | Keeps one auditable local source while supporting Vite, Wrangler, and the voice process | Secret handling | Project Lead |
| 2026-07-26 | DEC-015 | Provider free tiers are preferred; application-side usage accounting and stop checks enforce the approved project cap because provider budget alerts may not be hard stops | Prevents accidental spend while keeping live acceptance possible | Cost control | Project Lead; pending budget approval |
| 2026-07-26 | DEC-016 | P1 uses explicit Deepgram Nova-3 `zh-CN` or `en` session configuration; do not rely on `multi` for Chinese/English code-switching unless future official support and a live test prove it | Current Nova-3 supports Chinese separately, but its documented `multi` set does not include Chinese | Voice STT behavior | Project Lead |
| 2026-07-26 | DEC-017 | Use the direct LiveKit Deepgram plugin with the approved Deepgram key for Chinese; do not assume every LiveKit Inference route exposes the same Chinese model set | Current direct plugin/provider path documents Nova-3 Chinese support and aligned transcripts | Voice integration | Project Lead |
| 2026-07-26 | DEC-018 | Normalize model numbers, dates, currencies, and units in the approved TTS text before ElevenLabs Flash v2.5 synthesis | Flash v2.5 disables broad number normalization by default for latency, while the demo depends on accurate model/unit speech | Voice output | Project Lead |
| 2026-07-26 | DEC-019 | Do not create R11 ticket schema/UI during P0 or P1; add it only after G2 approval and the G3 entry check. Defer voice-only schema until P1 | Prevents optional or later-gate work from silently starting early | Migration sequencing | Project Lead |
| 2026-07-26 | DEC-020 | User-visible knowledge-source deletion is a soft-disable/tombstone plus retrieval exclusion and object-lifecycle action; cited chunks remain for audit until the conversation retention policy permits physical deletion | The blueprint's citation `ON DELETE RESTRICT` conflicts with immediate source-row cascade deletion | Knowledge lifecycle and citations | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-021 | Freeze additional derived acceptance fixtures before model calibration: extended in-scope questions, prompt-injection content, redirect/DNS SSRF cases, and the full R11 matrix only if G3 opens | The supplied fixtures cover core cases but do not satisfy every numeric matrix in the acceptance document | Evaluation integrity | Project Lead |
| 2026-07-26 | DEC-022 | Correct fixed case `OUT-08` to a genuinely unsupported high-altitude derating question while preserving the 12 in-scope/8 out-of-scope counts | The former food-grade question is directly answerable from the supplied manual and could not truthfully expect missing-knowledge handoff | Acceptance integrity | Project Lead |
| 2026-07-26 | DEC-023 | Browser-to-R2 upload uses Worker-issued short-lived, single-object signed PUT URLs with exact origin CORS; a bucket-scoped R2 S3 signer credential stays in Worker secrets and the browser never receives it | Completes the missing upload contract while preserving the controlled client-extraction design and signed-R2 invariant | P0 ingestion architecture | Project Lead |
| 2026-07-26 | DEC-024 | Public customers receive human messages through conversation-token Worker polling; only authenticated Admin/Agent clients use Supabase Realtime + RLS | Provides the required near-real-time handoff UX without granting public clients direct database access or adding a new realtime platform | P0 handoff architecture | Project Lead |
| 2026-07-26 | DEC-025 | Retrieval filters enabled current source/document/chunk rows, computes semantic plus exact/trigram lexical evidence, then applies the threshold to the combined score | Prevents disabled-document leakage and allows exact SKU/number evidence to rescue a weak semantic-only result | RAG migration design | Project Lead |
| 2026-07-26 | DEC-026 | Every AI-derived artifact links to a complete `ai_runs` audit row; Agent guardrail DTOs redact blocked candidate text and only Admin authorization may retrieve it | Satisfies the token/latency/cost audit contract and avoids relying on RLS for column-level secrecy | AI audit and authorization | Project Lead |
| 2026-07-26 | DEC-027 | Guardrail supervision runs after a candidate answer exists; only input prechecks/retrieval preparation may run in parallel, and audible TTS starts only after approval | The supervisor consumes the candidate, so full parallel execution is impossible and would violate the no-speech-before-guardrail invariant | Text and voice safety path | Project Lead |
| 2026-07-26 | DEC-028 | Warm voice latency evidence uses at least 40 counted turns, at least 20 per language, nearest-rank percentiles, browser playback start, retained raw traces, and separately reported failures/cold starts | Makes the G2 P50/P95 claim reproducible and prevents selective timing | P1 acceptance protocol | Project Lead |
| 2026-07-26 | DEC-029 | R11 requires G2 user acceptance and at least four uninterrupted working hours remaining when the entry decision is recorded | Resolves the ambiguous `3–4 hours` wording conservatively without risking P0/P1 evidence or fixes | Optional R11 gate | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-030 | Each human gate uses a fixed evidence bundle containing commit/deploy/migration/fixture identity, executed checks, security/evaluation evidence, costs, defects, and required demo-run traces | Converts subjective review wording into an auditable completion condition | G0/G1/G2 release evidence | Project Lead |
| 2026-07-26 | DEC-031 | G0 is approved with a USD 50 aggregate live-provider cap; approved subscriptions count inside the cap, every paid upgrade still requires separate approval, development-only resource creation and bounded smoke tests are authorized, local/dedicated-dev migrations and resets are authorized, non-production preview deployment is authorized, generated development secrets stay in ignored/provider stores, the closest available North American west Supabase region is used, retention is 30 days, and P0/P1 credentials may remain deferred until their live gates | Records the user's approval of the consolidated Gate 0 defaults while preserving production, secret, and spend boundaries | Day 1–10 execution authority and resource controls | Forrest Zhang |
| 2026-07-26 | DEC-032 | The Day 1 modular monolith uses `apps/web`, `apps/api`, and a disabled `apps/voice-agent` scaffold plus shared config/contracts/core/UI packages; the Worker serves the Vite build through Static Assets and owns `/api/*` and `/health` | Establishes a reviewable locked-stack foundation without starting P1 or R11 early | Repository structure and single-origin runtime | Project Lead |
| 2026-07-26 | DEC-033 | Every P0 tenant table has `organization_id`, explicit composite tenant foreign keys where applicable, RLS enabled and forced, no anonymous table grants, authenticated grants constrained by explicit policies, and server-only `service_role` grants; physical knowledge deletion remains unavailable to authenticated users | Converts the schema blueprint into enforceable tenant, privilege, and citation-retention boundaries | P0 database security | Project Lead |
| 2026-07-26 | DEC-034 | Local SmartService Supabase uses the isolated `55320`–`55329` port range and disables only the optional local analytics collector because Colima cannot mount its Docker socket; application `audit_logs`, `ai_runs`, and Cloudflare observability remain enabled | Avoids disrupting the unrelated project already using default ports and resolves a machine-specific non-product container failure | Local development only | Project Lead |
| 2026-07-26 | DEC-035 | Day 2 uses explicit `mock` or `live` ingestion-provider mode: deterministic local crawl/embedding/upload adapters are valid for routine tests, while production configured for mock mode fails closed | Preserves zero-cost reproducibility without allowing fixture behavior to masquerade as production integration | P0 provider configuration and acceptance evidence | Project Lead |
| 2026-07-26 | DEC-036 | Knowledge intake stores immutable versioned documents/chunks and switches the active version only inside atomic completion; retry idempotency returns the existing job, failed reprocessing preserves its intended target version, and the prior active version remains retrievable during processing or failure | Prevents duplicate rows, stale retry corruption, and a knowledge outage while a replacement version is built | P0 ingestion lifecycle and retrieval continuity | Project Lead |
| 2026-07-26 | DEC-037 | Browser-extracted PDF/DOCX content is treated as untrusted input. The Worker revalidates tenant object prefixes, signed upload metadata, actual size/hash, MIME and file signatures, extracted payload consistency, and locked limits before queueing | Client extraction improves demo performance but cannot serve as the authorization or integrity boundary | P0 file-ingestion security | Project Lead |
| 2026-07-26 | DEC-038 | The Day 2 acceptance corpus is generated deterministically as a real text PDF, heading-aware DOCX, and bounded same-origin site with a committed SHA-256 manifest; changing the corpus requires an explicit version/review rather than silent regeneration during calibration | Keeps extraction, retrieval, and later model evaluation evidence reproducible | P0 fixture integrity | Project Lead |
| 2026-07-26 | DEC-039 | Public conversation tokens use scoped HS256 claims with an organization, exact conversation subject, nonce, allowed scopes, and a two-hour default expiry; public clients never receive Supabase credentials or direct table access | Keeps the public demo stateless at the edge while constraining every read/write to one conversation | P0 public-chat authorization | Project Lead |
| 2026-07-26 | DEC-040 | A factual AI answer may persist only when every citation maps to the exact retrieved chunk set and the cited source/version is still enabled; the AI run, assistant message, citations, handoff, gap, and audit artifacts commit atomically | Prevents fabricated/stale evidence and partial turn records | P0 grounded-answer integrity | Project Lead |
| 2026-07-26 | DEC-041 | The live combined retrieval threshold remains `0.72`; deterministic local chat uses threshold `0` only because feature-hashed fixture embeddings validate orchestration and citation behavior, not live embedding-score calibration | Keeps zero-cost tests deterministic without presenting mock similarity scores as model-quality evidence | P0 evaluation boundary | Project Lead |
| 2026-07-26 | DEC-042 | Turnstile is validated server-side with timeout, bounded retry, expected action, and optional hostname; deterministic bypass is local-only, and production fails closed unless ingestion, chat, and Turnstile providers are all live | Enforces abuse controls at the trust boundary and prevents accidental public mock deployment | P0 public endpoint security | Project Lead |
| 2026-07-26 | DEC-043 | Public human-message delivery uses one-second cursor polling with conditional ETags and the scoped conversation token; responses expose public citation IDs and excerpts, never internal chunk/database evidence identifiers | Delivers a simple near-real-time public UX without direct database access or identifier leakage | P0 public messaging contract | Project Lead |
| 2026-07-26 | DEC-044 | Guardrail, handoff, assignment, human-message, closure, rule-management, and finalization state transitions are Worker-owned service operations; authenticated clients cannot mutate the underlying rows or directly query guardrail events | Keeps tenant and role authorization, redaction, validation, idempotency, and audit behavior at one trusted boundary | P0 operational security | Project Lead |
| 2026-07-26 | DEC-045 | Customer input is checked deterministically before retrieval; a candidate answer is checked deterministically and by the configurable auxiliary supervisor before delivery, and blocked candidate text is available only through an explicit Admin-authorized endpoint | Enforces every fixed rule without model dependence while retaining strict supervised coverage and protecting unsafe content | P0 guardrail path | Project Lead |
| 2026-07-26 | DEC-046 | Public conversation tokens remain valid for scoped reads after handoff or closure but cannot submit another AI turn once the conversation leaves `active_ai` | Allows the customer to receive human and final messages without permitting AI to continue after takeover | P0 handoff messaging | Project Lead |
| 2026-07-26 | DEC-047 | Incremental summaries and full handoff packages are derived from persisted conversation state at every relevant transition; unknown customer-card values are represented explicitly as not provided | Gives the Agent a deterministic, current handoff record without inventing customer facts | P0 handoff context | Project Lead |
| 2026-07-26 | DEC-048 | Finalization uses an ID-only Queue with single consumer concurrency, authoritative state reconciliation, and an existing-record check before any model call; the strict final record keeps `ticket` null until G3 opens R11 | Makes retries idempotent, bounds auxiliary-model cost, and prevents optional ticket scope from entering P0 | P0 finalization and R11 gate | Project Lead |
| 2026-07-26 | DEC-049 | Dashboard periods are inclusive at the UI calendar boundary and inclusive-from/exclusive-to in the Worker/database contract; conversation rates divide by closed conversations, containment requires a final `resolved_ai` outcome with no historical handoff, handoff counts any handoff record, and open gaps use `last_seen_at` in the selected period | Makes every displayed P0 metric deterministic, date-filterable, and reproducible | P0 dashboard metrics | Project Lead |
| 2026-07-26 | DEC-050 | Dashboard and knowledge-gap reads/actions are Admin-only Worker operations; direct authenticated gap reads/updates and all service aggregation, resolution, retrieval, and re-test functions are revoked | Prevents Agent access, cross-tenant leakage, forged analytics, and unaudited gap state changes | P0 analytics authorization | Project Lead |
| 2026-07-26 | DEC-051 | One-click gap resolution creates an idempotent manual knowledge source and shared ingestion job, keeps the gap Open while processing, and marks it Resolved only when that exact linked source reaches Ready | Reuses the proven ingestion pipeline and prevents an unembedded answer from being presented as a completed repair | P0 knowledge feedback loop | Project Lead |
| 2026-07-26 | DEC-052 | Gap re-test uses the original question and retrieves only from the linked ready manual source; answers require citations within that exact retrieval set and pass deterministic plus auxiliary output guardrails, while the deterministic adapter may return an exact Admin-authored Question/Answer section only when normalized questions match | Proves the repair itself without unrelated evidence and keeps zero-cost local acceptance faithful to the live contract | P0 knowledge-gap re-test | Project Lead |
| 2026-07-27 | DEC-053 | Forrest Zhang authorized Codex to own all testing and continue automatically through Days 6–10 without pausing at G1/G2 implementation review; local/mock evidence remains explicitly distinct from hosted-provider acceptance, and live deployment, secrets, paid upgrades, production actions, and R11 retain their existing gates | Preserves uninterrupted delivery without misrepresenting unavailable external-provider evidence or broadening production authority | Day 6–10 execution and review gates | Forrest Zhang |
| 2026-07-27 | DEC-054 | Voice startup creates the conversation and ten-minute room token only after an explicit customer click; Agent dispatch contains only `voiceSessionId`, the Agent retrieves tenant configuration through a distinct internal bearer boundary, microphone publication waits for a documented post-initialization Agent state, and local mock mode is forbidden in production | Keeps tenant identity and provider secrets server-side, prevents pre-click sessions or premature microphone capture, and separates reproducible lifecycle tests from live WebRTC/STT claims | P1 voice session security and lifecycle | Project Lead |
| 2026-07-27 | DEC-055 | Voice final transcripts call the same caller-authorized turn processor as text after service-token and session tenant resolution; only the resulting guardrail-approved public answer is sent to ElevenLabs Flash v2.5, speech is limited to two sentences with non-speech artifacts removed, and citations remain screen-only | Prevents text/voice policy drift, keeps unsafe candidates inaudible, and avoids speaking URLs, IDs, or serialized internal data | P1 shared assistant and TTS boundary | Project Lead |
| 2026-07-27 | DEC-056 | P1 uses LiveKit's bundled multilingual turn detector with adaptive interruption, a 500-millisecond minimum, 2-second false-interruption resume, dynamic endpointing, and preemptive generation while preemptive TTS remains off; browser playback starts are detected from received audio energy, and local mock clocks are never eligible for G2 | Preserves native cancellation/backchannel behavior and guardrail-before-audio safety while keeping latency claims evidence-honest | P1 turn, interruption, and latency evidence | Project Lead |
| 2026-07-27 | DEC-057 | Voice handoff and terminal provider failure speak only a fixed or shared-guardrail-approved safe phrase, then shut down after playout; obsolete Worker turns are aborted, room recovery uses native reconnect followed by at most two token-refresh attempts, and no retry can restart AI after handoff | Prevents stale speech, infinite reconnect loops, stack leakage, and accidental AI continuation while preserving a recoverable transient-network path | P1 voice handoff and failure recovery | Project Lead |
| 2026-07-27 | DEC-058 | R11 does not enter the Day 10 release because live G2 has not been accepted; keep `ENABLE_R11_TICKET_UI=false` and introduce no ticket migration, classification call, list, filter, or detail UI | The locked G3 conditions are not met, so optional scope cannot displace P0/P1 evidence or create a misleading acceptance claim | Optional R11 entry gate | Project Lead |
| 2026-07-27 | DEC-059 | The local/mock UAT release is versioned and tagged `0.10.0`; its three clean full demos and retained evidence qualify it for local functional UAT but not hosted G1, live G2, production, or public deployment | Creates an immutable, reproducible handoff while preserving every external-provider and authorization boundary | Day 10 release and UAT handoff | Project Lead |
| 2026-07-27 | DEC-060 | Local live-provider setup may proceed with bounded smoke tests under the approved USD 50 cap, but the evidence remains split: OpenAI, LiveKit, Deepgram, and ElevenLabs are locally verified; hosted Supabase/Cloudflare, deployed Agent, final device evidence, and commercial-use confirmation remain required before G1/G2 claims | Prevents partial credential success from being reported as full hosted or live voice acceptance | Live provider setup and acceptance evidence | Forrest Zhang |
| 2026-07-27 | DEC-061 | ElevenLabs is retained for P1 TTS while Deepgram remains STT; replacing ElevenLabs with another TTS provider would be an architecture change requiring explicit approval. The development key is least-privilege for Text to Speech, Voices Read, and Models access, so commercial-use confirmation remains a separate account-plan check | Preserves the locked voice pipeline and avoids broad API permissions just to read account metadata | P1 voice provider boundary | Project Lead |
| 2026-07-27 | DEC-062 | The local LiveKit Agent entrypoint is split from the CLI launcher, and session log/trace recording export is disabled while audio recording remains off | LiveKit's worker loader requires a pure Agent definition default export, and the current project rejects cloud recording exports because data recording is disabled; SmartService keeps its own bounded runtime metrics instead | P1 local live voice readiness | Project Lead |
| 2026-07-27 | DEC-063 | Do not use the existing Supabase `HelloX Delivery Hub` browser-visible project for SmartService hosted DEV without explicit approval; the dedicated connector-created SmartService project is the intended DEV target, but hosted Worker deployment remains blocked until its service-role key is stored safely | Prevents unrelated-data/schema risk while keeping the hosted evidence boundary clear | Hosted Supabase provisioning | Project Lead |
| 2026-07-27 | DEC-064 | The public chat should not display a permanent transfer-to-human button on first load; contextual human support is offered after explicit frustration, repeated clarifications, or request failure, and the server still owns explicit handoff-request detection and all state transitions | Avoids encouraging unnecessary handoff while preserving customer escape and safety escalation | P0 public handoff UX | Project Lead |
| 2026-07-27 | DEC-065 | Supersede DEC-063: the connector-created Supabase project `wfkheempcfislbaonkiz` was in the wrong account and has been paused/inactivated; use the browser-created Supabase `SmartService` project `ibuvpregltbvxsxhivrg` in org `etsklszncywvjfxcsyls` for hosted DEV | Aligns hosted resources with the account the user can see and operate while preserving deletion limitations for the hidden connector account | Hosted Supabase provisioning | Forrest Zhang |
| 2026-07-27 | DEC-066 | Connect the existing Cloudflare Worker `smartservice-dev` to native Workers Builds for `helloxtech/SmartService` on production branch `main`; keep root directory `/`, build command `pnpm --filter @smartservice/web build`, and deploy command `pnpm --filter @smartservice/api exec wrangler deploy` | Satisfies the requested Git-triggered DEV deployment path without creating a duplicate Worker; preview branch version command remains the Cloudflare default until a non-production branch build is needed | Hosted DEV deployment | Forrest Zhang |
| 2026-07-28 | DEC-067 | Seed hosted DEV with fictional Smart Service PDF/DOCX/URL fixture knowledge through the shared deterministic chunking, embedding, and database-completion path for mock-provider text UAT smoke | Makes the hosted URL useful for product review while explicitly preserving live R2 signed upload, Browser Run crawl, live Turnstile, and live model/provider-mode checks as G1 blockers | Hosted DEV UAT readiness | Forrest Zhang |
| 2026-07-29 | DEC-068 | Browser Supabase sign-in configuration is served from the Worker runtime through `/api/public-config` instead of relying only on Vite build-time `VITE_SUPABASE_*` values | Cloudflare Git Builds do not read local `.env.local`, so runtime delivery keeps hosted `smartservice.ca` and `workers.dev` sign-in functional after automatic Git deployments while exposing only browser-safe Supabase URL and anon key | Hosted DEV sign-in configuration | Project Lead |
| 2026-07-29 | DEC-069 | Public customers may continue adding messages in `handoff_requested` and `active_human`; those updates persist for the Agent handoff context without running retrieval, guardrails, LLM, or TTS, while human replies remain restricted to the claiming Agent/Admin | Opening a handoff conversation must not imply claim ownership, but the customer should still be able to add useful details while waiting or after a human connects | P0 handoff messaging UX | Forrest Zhang |
| 2026-07-30 | DEC-070 | Superseded by DEC-074: the earlier fictional demo-tenant naming pass is retired; Chinese support remains delivered as a visible language switch rather than simultaneous bilingual labels | Keeps the localization decision while removing the rejected Flow-style visible brand | Demo branding and Chinese UI | Forrest Zhang |
| 2026-07-30 | DEC-071 | Until every hosted widget has the current demo key, the public text and voice entrypoints try the current demo public key first and retry the legacy demo key only when the Worker returns `WIDGET_NOT_FOUND` | Allows the approved Git/Cloudflare deploy to proceed without breaking hosted UAT if Supabase still has the old public key, while keeping the fallback narrow and removable after hosted data is refreshed | Hosted demo deployment bridge | Forrest Zhang |
| 2026-07-30 | DEC-072 | Supersede the visible-copy part of DEC-070: Chinese/English support is presented through a customer-selectable UI language switch, not simultaneous bilingual labels; the first localization pass covers the sign-in shell, public chat, voice page, and team navigation while deeper admin operation panels remain English for this UAT build | Matches Forrest's requested UAT behavior without delaying the deploy on a full i18n rewrite of every internal admin table and form | SmartService UI localization | Forrest Zhang |
| 2026-07-30 | DEC-073 | Authenticated workspace UX should prioritize a wider working area, language-switchable Admin operation panels, and an Agent handoff package that exposes existing summaries, deterministic next actions, citations, and source links without extra AI calls | Hosted UAT showed the prior workspace was cramped, mixed languages in Chinese mode, and gave Agents too little immediately actionable context | Workspace UI/UX and handoff package | Forrest Zhang |
| 2026-07-30 | DEC-074 | Supersede DEC-070 for browser-visible branding: the product, tenant display, customer chat, assistant greeting, evidence labels, UAT docs, and hosted data use `Smart Service`; legacy low-level demo public keys may remain unchanged temporarily only as non-visible compatibility identifiers | Forrest rejected Flow-style demo names and requested one professional Smart Service identity while keeping hosted UAT stable | Smart Service branding and UX | Forrest Zhang |
| 2026-07-31 | DEC-075 | Use free provider plans where they can satisfy acceptance, but leave explicit placeholders where free tiers or account permissions cannot satisfy the gate. P0 remains blocked on missing Cloudflare Browser Run token, R2 S3 signer credentials, and hostname Turnstile keys. P1 Agent packaging is ready, but LiveKit Cloud Agent deployment returned an early-access-not-enabled blocker, and ElevenLabs Free still cannot support commercial/sales-facing G2 output | Preserves Forrest's free-plan instruction without weakening app secrets, misusing broad OAuth tokens, or claiming live acceptance from unavailable provider capabilities | P0/P1 live acceptance boundary | Forrest Zhang |
| 2026-07-31 | DEC-076 | Hosted smoke verification may prove current UAT readiness through the public single-origin path when the online service-role key is not present locally. Direct hosted Supabase seed/count remains a separate admin check and must not be represented as available until the online service-role key is stored locally or connector SQL works for project `ibuvpregltbvxsxhivrg` | Keeps UAT smoke repeatable without requiring secret extraction from the browser, while preserving the evidence boundary for privileged database administration | Hosted DEV verification boundary | Project Lead |
| 2026-08-01 | DEC-077 | Install one globally mounted, vendored HelloX Feedback `0.2.0` widget for both anonymous and authenticated intake at the exact `https://smartservice.ca` origin. Public chat and voice routes default to anonymous mode with Turnstile and no attachments; signed-in identity is exchanged only by the same-origin Worker through a short-lived single-use session, and the Delivery Hub server key remains a Cloudflare secret | Provides feedback throughout SmartService without exposing privileged credentials or weakening anonymous abuse and attachment controls | Hosted SmartService feedback integration | Forrest Zhang |
| 2026-08-01 | DEC-078 | Prepare the vendored HelloX Feedback `0.2.1` upgrade in an isolated worktree. The component converts Tailwind 4 computed `oklch()`/`oklab()` colors only inside its detached screenshot clone, labels every marker explicitly, and keeps the first marker as the ticket-title source; no SmartService product scope, credential, or hosted runtime setting changes | Restores automatic evidence capture on SmartService's Tailwind 4 UI and prevents Marker 1 from appearing missing while keeping the upgrade independently mergeable | Local SmartService feedback component upgrade | Forrest Zhang |
| 2026-08-01 | DEC-079 | Promote the exact verified HelloX Feedback `0.2.1` vendored bundle through the existing Git-triggered `main` deployment, retain the current hosted DEV/mock-provider boundary for the rest of SmartService, and accept the live release only after `smartservice.ca` returns the reviewed SHA-256 and a real browser produces an automatic screenshot with two retained markers | Makes the requested Feedback correction usable without incorrectly expanding the release into a full SmartService G1/G2 production-readiness claim | Hosted SmartService Feedback 0.2.1 release | Forrest Zhang |
| 2026-08-01 | DEC-080 | Remove the editable `Email for updates` field in HelloX Feedback `0.2.2`. SmartService requires every authenticated member to have a verified Supabase account email and sends that email only through the server-to-server identity exchange; anonymous feedback contains no identity or contact email | Avoids asking signed-in users for data already guaranteed by invitation-based accounts, prevents browser spoofing, and avoids an anonymous unwanted-contact surface | SmartService Feedback identity and contact boundary | Forrest Zhang |
| 2026-08-01 | DEC-081 | Upgrade the local SmartService candidate to HelloX Feedback `0.2.3`. The launcher starts fully labeled, collapses after five idle seconds, expands immediately on pointer hover or keyboard focus, and re-collapses after two seconds; an active or minimized report remains fully labeled | Teaches first-time users what the persistent control does, then reduces obstruction without sacrificing keyboard access, touch target size, or draft continuity | SmartService Feedback launcher UX | Forrest Zhang |
| 2026-08-02 | DEC-082 | Supersede DEC-041 for hosted live retrieval: decouple upload, crawl, and embedding provider modes; enable live OpenAI chat, auxiliary review, and embeddings plus Cloudflare Browser Run crawling in hosted DEV; use a corpus-calibrated `0.35` retrieval threshold; and add bounded recent context only for clearly dependent follow-up questions | Hosted user evidence proved that the legacy shared mock mode silently replaced real websites with fixture content, the `0.72` threshold rejected supported bilingual questions, and context-free retrieval could not validate confirmations such as `are you sure?`; split modes preserve the current development upload adapter while restoring real grounded answers without weakening citation validation or out-of-scope handoff | Hosted knowledge ingestion and retrieval | Forrest Zhang |
| 2026-08-02 | DEC-083 | Use Cloudflare Browser Run `/crawl` with `render: false` for bounded public knowledge websites, only documented crawl-specific controls, status-first polling that ignores incomplete queued records until completion, per-record validation at completion, and credential-safe provider status/error-code logs | The hosted integration exposed contract drift: the current static WordPress corpus does not need browser rendering, the unsupported request allow-pattern and validation of partial queued records caused fail-closed ingestion, and static crawling materially reduces subresource/SSRF surface and beta cost while same-origin patterns, public-DNS validation, completed-record validation, post-filtering, and DNS revalidation remain enforced | Browser Run live crawl contract | Forrest Zhang |
| 2026-08-02 | DEC-084 | Run GPT-5 family Structured Outputs with explicit `low` reasoning effort, a 2,500-token answer/finalization budget, a 1,500-token guardrail budget, and metadata-only incomplete-response diagnostics | The first live grounded turn retrieved eight valid chunks but returned no output text because the prior 1,000-token cap also covered the model's default reasoning tokens; explicit low effort preserves bounded cost/latency while leaving enough visible-output budget for the required strict schemas, and diagnostics expose only status/type/token metadata rather than prompts or customer content | Live OpenAI response reliability | Forrest Zhang |
| 2026-08-02 | DEC-085 | Resolve an exact approved manual Question/Answer and its immediately following confirmation deterministically from the retrieved manual chunk, while retaining citation validation and both output guardrail layers; use the live model for elaboration or any non-exact question | Admin approval already establishes the exact answer, so asking GPT-5 to rewrite it added citation-format failure risk and cost without adding knowledge; the bounded confirmation path directly solves `are you sure?` while requiring that the most recent customer question match the retrieved manual Question | Approved manual-answer reliability | Forrest Zhang |
| 2026-08-02 | DEC-086 | Disable, but do not delete, the three obsolete hosted fixture sources after the user-supplied website, DOCX, and manual answer pass live acceptance | Prevents fictional seed content and failed legacy reprocessing states from contaminating real tenant retrieval while preserving a reversible audit trail | Hosted knowledge-source governance | Project Lead |
| 2026-08-03 | DEC-087 | Evaluate `unsupported_claim` output rules against only the one-to-five retrieved chunks cited by the validated candidate, with each chunk bounded to 4,000 characters; cited evidence may establish factual support but never override price, delivery, competitor, security, safety, or other rules | The live English course answer was correctly retrieved and generated but intermittently blocked because the auxiliary supervisor saw no evidence and guessed that specific offerings were unsupported | Evidence-aware output supervision | Project Lead |
| 2026-08-03 | DEC-088 | Merge public-chat polling and send-completion messages by authoritative persisted message ID before rendering | Longer supervised turns exposed a race where the same AI message arrived through both paths and appeared twice despite the separate seen-ID ref | Public chat message consistency | Project Lead |
| 2026-08-03 | DEC-089 | Missing, insufficient, conflicting, or temporarily unavailable knowledge returns a localized non-terminal clarification and records a knowledge gap while the conversation remains `active_ai`; only an explicit customer request or a safety/guardrail rule may initiate handoff | The customer did not request a human, and an unsupported follow-up must not silently remove AI access or claim that a transfer was requested | Customer-controlled handoff policy | Project Lead |
| 2026-08-03 | DEC-090 | A customer may start a new conversation from any restored chat state through a visible, confirmed action above the composer. The reset clears only that browser tab's scoped session, retains the prior server conversation for Agent support and audit, requires a fresh verification, and creates a new server conversation on the customer's next message | A pending handoff previously trapped returning customers in the restored read-only conversation with no route back to AI, while deleting the support record would lose legitimate history and Agent context | Public chat session lifecycle | Project Lead |
| 2026-08-03 | DEC-091 | Use Cloudflare-hosted `@cf/zai-org/glm-4.7-flash` in non-thinking mode as the primary live grounded-answer model, with one bounded `gpt-5-mini` fallback after provider, schema, or exact-citation validation failure. Keep deterministic manual answers, retrieval gates, customer-controlled handoff, and both guardrail layers unchanged; persist the actual successful provider/model and disable provider-side content logging | Lowers routine answer cost without weakening evidence enforcement or making a single lower-cost provider an availability dependency; successful and failed primary attempts remain observable through content-free model/token/error metadata | Live grounded-answer inference, reliability, privacy, and cost control | Forrest Zhang |
| 2026-08-03 | DEC-092 | Temporarily set hosted `CHAT_FALLBACK_PROVIDER=none` while primary-model testing is active. Keep the OpenAI fallback implementation available behind the reversible `openai` setting, but never call it in `none` mode; a GLM provider or validation failure must fail closed into the existing AI-active safe clarification path | Makes every successful test answer attributable to GLM without removing the already-tested reliability option or changing deterministic manual answers, OpenAI embeddings, auxiliary supervision, citations, guardrails, or customer-controlled handoff | Hosted primary-model evaluation | Forrest Zhang |
| 2026-08-03 | DEC-093 | Treat customer questions as explicit answerable parts: decompose bounded multi-question turns, append English search hints to common Chinese school intents while preserving the original text, use a `0.30` threshold only for those cross-language expansions, interleave evidence across parts, and require exact Guqin/Guzheng entity matches. Customer copy must describe what was found naturally, answer requested titles directly, preserve supported partial answers through supervision, link citations to the exact crawled page, and retry the same GLM model once after provider/schema/citation failure before clarifying safely | Live review showed three independent failures: Guqin retrieved adjacent Guzheng evidence, four school questions diluted one embedding below threshold even though the approved About/address chunks existed, and accurate missing-field language was misclassified as an unsupported claim. The bounded same-model retry preserves primary-only attribution while reducing transient Structured Output failures | Humanized grounded multi-intent answers | Forrest Zhang |
| 2026-08-06 | DEC-094 | Customer-visible text and voice conversations speak as the current company's online customer-service team. Confirmed company facts are stated directly, never introduced as external research; copy must not expose an AI/bot identity, retrieval/evidence language, instructions to retry, or instructions to contact the company. When a detail needs confirmation, offer a customer-selected support-specialist follow-up. The existing explicit-choice handoff boundary remains in force, while safety guardrails may still initiate a protected escalation. | SmartService is tenant-generic: a music school, sales business, or future company must each receive a company-owned customer-service experience without weakening evidence-only answers, customer-controlled normal handoff, or safety escalation. A single prompt, response normalizer, safe clarification path, safety-copy normalizer, public composer, and voice copy keep that policy consistent across future turns. | Tenant customer-service ownership and specialist escalation | Forrest Zhang |
| 2026-08-06 | DEC-095 | Keep `CHAT_FALLBACK_PROVIDER=none` during primary-model testing. The second Cloudflare Workers AI attempt must be a classified corrective request rather than an identical deterministic replay: response-format, grounding/citation, and provider failures receive distinct tenant-neutral repair instructions while retaining the same model, evidence, two-attempt ceiling, and citation validation. If the turn still fails, persist and log the exact processing stage and responsible component; expose a company-service clarification, create no system-error knowledge gap, and initiate no handoff. Retrieval expansion must use broad business intents instead of a school-only gate, preserve exact letter-number product/plan identifiers across multipart questions, and treat requested current role-holders generically rather than hard-coding principal/founder wording. | The observed Guzheng failure was transient processing failure despite approved knowledge being retrievable. An identical retry and one catch-all customer message obscured the real failure, while school-only query and role rules would repeat the same patch-by-example mistake for future sales, appointment, product, or service tenants. Stage-aware audit, same-provider correction, and generic intent/entity/role safeguards improve reliability without hiding Cloudflare failures behind OpenAI or weakening fail-closed evidence rules. | Tenant-generic grounded-answer reliability, retrieval, and observability | Forrest Zhang |
