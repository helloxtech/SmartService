# Durable Project Decisions

> Append only. Record decisions that future Codex turns must know without rereading chat history.

| Date | ID | Decision | Reason | Scope impact | Approved by |
|---|---|---|---|---|---|
| 2026-07-26 | DEC-001 | P0 one week, P1 one week; R11 only after P0/P1 acceptance and if time remains | User-approved delivery constraint | Locks scope and priority | Forrest Zhang |
| 2026-07-26 | DEC-002 | Use the stack and boundaries in `docs/spec/00_CODEX_START_HERE.md` | Reduce research and implementation churn | Architecture locked | Forrest Zhang |
| 2026-07-26 | DEC-003 | One persistent Codex lead executes sequential vertical slices; separate commits and human gates remain | Minimize user prompting while preserving reviewability | Delivery workflow | Forrest Zhang |
| 2026-07-26 | DEC-004 | Product display name is `SmartService`, repository/package slug is `smartservice`, and new Cloud resources use `smartservice-*` names | Direct Gate 0 identity instruction | Documentation, packages, UI, deployment resources | Forrest Zhang |
| 2026-07-26 | DEC-005 | Historical source PDFs retain their original Chinese titles; repository-owned specifications use SmartService identity | Preserve source provenance while removing obsolete working-name drift | Documentation only | Forrest Zhang |
| 2026-07-26 | DEC-006 | NovaFlow fixtures are the default fictional demo tenant; real customer, production, regulated, or sensitive data is not permitted without a separate review | Keeps the sales demo reproducible and low risk | Demo data and privacy | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-007 | The default G1 runtime is local unless an externally reachable preview is explicitly approved; a bare `workers.dev` URL is non-production but not private | Avoid accidentally exposing a supposedly private preview | Deployment and security | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-008 | If a preview is approved, prefer one Cloudflare Worker with Static Assets and the Hono API under one origin; Queue consumers and provider modules remain inside the modular monolith | Reduces CORS, version, and deployment drift without changing the locked stack | P0 deployment topology | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-009 | P1 `<1.5s` is the P95 warm-turn target, not a universal guarantee; missing it requires a complete honest latency report and explicit G2 acceptance rather than hidden/selective results | Resolves source wording against the controlling prompt and acceptance specification | Voice acceptance | Forrest Zhang via controlling prompt |
| 2026-07-26 | DEC-010 | Canonical model variables are `OPENAI_CHAT_MODEL`, `OPENAI_SUPERVISOR_MODEL`, `OPENAI_EMBEDDING_MODEL`, and `OPENAI_EMBEDDING_DIMENSIONS`; model aliases remain configurable and undated | Removes conflicting environment names and prevents unavailable snapshot pins | Configuration | Project Lead |
| 2026-07-26 | DEC-011 | Cloudflare `/crawl` uses the REST API and a least-privilege Browser Run token; a Browser binding is not treated as equivalent | Matches the current provider contract and makes credentials auditable | URL ingestion | Project Lead |
| 2026-07-26 | DEC-012 | Firecrawl remains an inactive optional adapter; a real account or usage requires a demonstrated Browser Run blocker and separate provider/cost approval | Preserves the locked provider choice and avoids scope/cost creep | URL ingestion fallback | Project Lead |
| 2026-07-26 | DEC-013 | Audio recording remains disabled; application records default to the documented 30-day demo retention unless Gate 0 changes it | Minimizes privacy exposure while retaining the specification default | Privacy and data lifecycle | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-014 | Root `.env.local` is the canonical local secret source; app-specific ignored files or explicit workspace configuration may consume it, but no secret is copied into tracked configuration | Keeps one auditable local source while supporting Vite, Wrangler, and the voice process | Secret handling | Project Lead |
| 2026-07-26 | DEC-015 | Provider free tiers are preferred; application-side usage accounting and stop checks enforce the approved project cap because provider budget alerts may not be hard stops | Prevents accidental spend while keeping live acceptance possible | Cost control | Project Lead; pending budget approval |
| 2026-07-26 | DEC-016 | P1 uses explicit Deepgram Nova-3 `zh-CN` or `en` session configuration; do not rely on `multi` for Chinese/English code-switching unless future official support and a live test prove it | Current Nova-3 supports Chinese separately, but its documented `multi` set does not include Chinese | Voice STT behavior | Project Lead |
| 2026-07-26 | DEC-017 | Use the direct LiveKit Deepgram plugin with the approved Deepgram key for Chinese; do not assume every LiveKit Inference route exposes the same Chinese model set | Current direct plugin/provider path documents Nova-3 Chinese support and aligned transcripts | Voice integration | Project Lead |
| 2026-07-26 | DEC-018 | Normalize model numbers, dates, currencies, and units in the approved TTS text before ElevenLabs Flash v2.5 synthesis | Flash v2.5 disables broad number normalization by default for latency, while the demo depends on accurate model/unit speech | Voice output | Project Lead |
| 2026-07-26 | DEC-019 | Do not create R11 ticket schema/UI during P0 or P1; add it only after G2 approval and the G3 entry check. Defer voice-only schema until P1 | Prevents optional or later-gate work from silently starting early | Migration sequencing | Project Lead |
| 2026-07-26 | DEC-020 | User-visible knowledge-source deletion is a soft-disable/tombstone plus retrieval exclusion and object-lifecycle action; cited chunks remain for audit until the conversation retention policy permits physical deletion | The blueprint's citation `ON DELETE RESTRICT` conflicts with immediate source-row cascade deletion | Knowledge lifecycle and citations | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-021 | Freeze additional derived acceptance fixtures before model calibration: extended in-scope questions, prompt-injection content, redirect/DNS SSRF cases, and the full R11 matrix only if G3 opens | The supplied fixtures cover core cases but do not satisfy every numeric matrix in the acceptance document | Evaluation integrity | Project Lead |
| 2026-07-26 | DEC-022 | Correct fixed case `OUT-08` to a genuinely unsupported high-altitude derating question while preserving the 12 in-scope/8 out-of-scope counts | The former food-grade question is directly answerable from the supplied manual and could not truthfully expect missing-knowledge handoff | Acceptance integrity | Project Lead |
| 2026-07-26 | DEC-023 | Browser-to-R2 upload uses Worker-issued short-lived, single-object signed PUT URLs with exact origin CORS; a bucket-scoped R2 S3 signer credential stays in Worker secrets and the browser never receives it | Completes the missing upload contract while preserving the controlled client-extraction design and signed-R2 invariant | P0 ingestion architecture | Project Lead |
| 2026-07-26 | DEC-024 | Public customers receive human messages through conversation-token Worker polling; only authenticated Admin/Agent clients use Supabase Realtime + RLS | Provides the required near-real-time handoff UX without granting public clients direct database access or adding a new realtime platform | P0 handoff architecture | Project Lead |
| 2026-07-26 | DEC-025 | Retrieval filters enabled current source/document/chunk rows, computes semantic plus exact/trigram lexical evidence, then applies the threshold to the combined score | Prevents disabled-document leakage and allows exact SKU/number evidence to rescue a weak semantic-only result | RAG migration design | Project Lead |
| 2026-07-26 | DEC-026 | Every AI-derived artifact links to a complete `ai_runs` audit row; Agent guardrail DTOs redact blocked candidate text and only Admin authorization may retrieve it | Satisfies the token/latency/cost audit contract and avoids relying on RLS for column-level secrecy | AI audit and authorization | Project Lead |
| 2026-07-26 | DEC-027 | Guardrail supervision runs after a candidate answer exists; only input prechecks/retrieval preparation may run in parallel, and audible TTS starts only after approval | The supervisor consumes the candidate, so full parallel execution is impossible and would violate the no-speech-before-guardrail invariant | Text and voice safety path | Project Lead |
| 2026-07-26 | DEC-028 | Warm voice latency evidence uses at least 40 counted turns, at least 20 per language, nearest-rank percentiles, browser playback start, retained raw traces, and separately reported failures/cold starts | Makes the G2 P50/P95 claim reproducible and prevents selective timing | P1 acceptance protocol | Project Lead |
| 2026-07-26 | DEC-029 | R11 requires G2 user acceptance and at least four uninterrupted working hours remaining when the entry decision is recorded | Resolves the ambiguous `3–4 hours` wording conservatively without risking P0/P1 evidence or fixes | Optional R11 gate | Project Lead; pending G0 confirmation |
| 2026-07-26 | DEC-030 | Each human gate uses a fixed evidence bundle containing commit/deploy/migration/fixture identity, executed checks, security/evaluation evidence, costs, defects, and required demo-run traces | Converts subjective review wording into an auditable completion condition | G0/G1/G2 release evidence | Project Lead |
| 2026-07-26 | DEC-031 | G0 is approved with a USD 50 aggregate live-provider cap; approved subscriptions count inside the cap, every paid upgrade still requires separate approval, development-only resource creation and bounded smoke tests are authorized, local/dedicated-dev migrations and resets are authorized, non-production preview deployment is authorized, generated development secrets stay in ignored/provider stores, the closest available North American west Supabase region is used, retention is 30 days, and P0/P1 credentials may remain deferred until their live gates | Records the user's approval of the consolidated Gate 0 defaults while preserving production, secret, and spend boundaries | Day 1–10 execution authority and resource controls | Forrest Zhang |
| 2026-07-26 | DEC-032 | The Day 1 modular monolith uses `apps/web`, `apps/api`, and a disabled `apps/voice-agent` scaffold plus shared config/contracts/core/UI packages; the Worker serves the Vite build through Static Assets and owns `/api/*` and `/health` | Establishes a reviewable locked-stack foundation without starting P1 or R11 early | Repository structure and single-origin runtime | Project Lead |
| 2026-07-26 | DEC-033 | Every P0 tenant table has `organization_id`, explicit composite tenant foreign keys where applicable, RLS enabled and forced, no anonymous table grants, authenticated grants constrained by explicit policies, and server-only `service_role` grants; physical knowledge deletion remains unavailable to authenticated users | Converts the schema blueprint into enforceable tenant, privilege, and citation-retention boundaries | P0 database security | Project Lead |
| 2026-07-26 | DEC-034 | Local SmartService Supabase uses the isolated `55320`–`55329` port range and disables only the optional local analytics collector because Colima cannot mount its Docker socket; application `audit_logs`, `ai_runs`, and Cloudflare observability remain enabled | Avoids disrupting the unrelated project already using default ports and resolves a machine-specific non-product container failure | Local development only | Project Lead |
